@implements IDisposable
@inject NavigationManager NavigationManager
@inject IStringLocalizer<SharedResource> L

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">@L["Brand"]</a>
    </div>
</div>

<input type="checkbox" title="Menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> @L["Nav_Home"]
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="bills">
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> @L["Nav_Bills"]
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="members">
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> @L["Nav_Members"]
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="initiatives">
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> @L["Nav_Initiatives"]
            </NavLink>
        </div>

        <hr class="text-secondary mx-3 my-2" />

        <AuthorizeView>
            <Authorized>
                <div class="nav-item px-3">
                    <NotificationBell />
                </div>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="Account/Manage">
                        <span class="bi bi-person-fill-nav-menu" aria-hidden="true"></span> @context.User.Identity?.Name
                    </NavLink>
                </div>
                <div class="nav-item px-3">
                    <form action="Account/Logout" method="post">
                        <AntiforgeryToken />
                        <input type="hidden" name="ReturnUrl" value="@currentUrl" />
                        <button type="submit" class="nav-link">
                            <span class="bi bi-arrow-bar-left-nav-menu" aria-hidden="true"></span> @L["Nav_Logout"]
                        </button>
                    </form>
                </div>
            </Authorized>
            <NotAuthorized>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="Account/Register">
                        <span class="bi bi-person-nav-menu" aria-hidden="true"></span> @L["Nav_Register"]
                    </NavLink>
                </div>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="Account/Login">
                        <span class="bi bi-person-badge-nav-menu" aria-hidden="true"></span> @L["Nav_Login"]
                    </NavLink>
                </div>
            </NotAuthorized>
        </AuthorizeView>

        <hr class="text-secondary mx-3 my-2" />

        <div class="px-3 pb-3 d-flex gap-1 flex-wrap">
            <a class="btn btn-sm btn-outline-light" href="/set-culture?culture=he&redirectUri=/@currentUrl">עב</a>
            <a class="btn btn-sm btn-outline-light" href="/set-culture?culture=ar&redirectUri=/@currentUrl">ع</a>
            <a class="btn btn-sm btn-outline-light" href="/set-culture?culture=ru&redirectUri=/@currentUrl">РУ</a>
            <a class="btn btn-sm btn-outline-light" href="/set-culture?culture=en&redirectUri=/@currentUrl">EN</a>
        </div>
    </nav>
</div>

@code {
    private string? currentUrl;

    protected override void OnInitialized()
    {
        currentUrl = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
        NavigationManager.LocationChanged += OnLocationChanged;
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        currentUrl = NavigationManager.ToBaseRelativePath(e.Location);
        StateHasChanged();
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= OnLocationChanged;
    }
}
